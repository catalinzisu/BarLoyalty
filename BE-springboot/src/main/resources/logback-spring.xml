<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_FILE" value="logs/application.log"/>
    <property name="LOKI_URL" value="${LOKI_URL:-http://localhost:3100}"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/application-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>10</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Loki Appender (Optional - requires Loki server running) -->
    <appender name="LOKI" class="com.grafana.loki.logback.appender.LokiAppender">
        <url>${LOKI_URL}/loki/api/v1/push</url>
        <batchSize>100</batchSize>
        <batchTimeoutMs>10000</batchTimeoutMs>
        <label>
            <name>app</name>
            <value>bar-loyalty</value>
        </label>
        <label>
            <name>environment</name>
            <value>${ENVIRONMENT:-development}</value>
        </label>
    </appender>

    <!-- Root Logger Configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <!-- Uncomment to enable Loki logging -->
        <!-- <appender-ref ref="LOKI"/> -->
    </root>

    <!-- Application-specific loggers -->
    <logger name="org.example.bespringboot" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="DEBUG"/>

    <!-- Spring profiles -->
    <springProfile name="development">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <springProfile name="production">
        <root level="INFO">
            <appender-ref ref="FILE"/>
            <!-- <appender-ref ref="LOKI"/> -->
        </root>
    </springProfile>
</configuration>

